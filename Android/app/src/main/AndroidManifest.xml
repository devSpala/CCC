<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HarnessApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".IpcService"
            android:process=":ipc_process"
            android:exported="false" />

        <!-- L2 persistent service in a SEPARATE PROCESS (":bridge").
             This is the structural analogue of the Windows WPF AppService provider (Pid2):
             it survives the UI process's Doze/standby transitions independently. -->
        <service
            android:name=".BridgeService"
            android:process=":bridge"
            android:exported="true" />

        <!-- adb-driven control surface. exported=true so `am broadcast` can reach it.
             (Test harness only — do not ship exported receivers in production.) -->
        <receiver
            android:name=".ControlReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.myapplication.SWEEP" />
                <action android:name="com.example.myapplication.RECONNECT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>